import type { FplSeasonRef } from '../../src/domain/fpl-season';

export const TEST_SEASON: FplSeasonRef = Object.freeze({
  seasonId: 2026,
  seasonCode: '2627',
});

export const HISTORICAL_TEST_SEASON: FplSeasonRef = Object.freeze({
  seasonId: 2025,
  seasonCode: '2526',
});
