# Copy to .env.migrate on the deployment host. This file is loaded only by
# the one-shot migration container and must never be mounted into API/workers.
# Use the direct Supabase `postgres` connection, not a runtime LOGIN or pooler.
DATABASE_URL="postgresql://postgres:replace@db.example.supabase.co:5432/postgres?sslmode=require"

# Required only when running the one-time bug-report storage dry-run/apply
# through `bun run db:migrate -- --storage-migration`. Keep these values in
# sync with the Data runtime and Web internal storage endpoint.
BUG_REPORT_STORAGE_INTERNAL_URL="https://your-frontend.example.com/api/internal/bug-report-storage"
BUG_REPORT_CLEANUP_SECRET="replace_with_shared_hmac_secret"
BUG_REPORT_STORAGE_LEGACY_ORIGIN="https://example.supabase.co"

# Ordinary migration and deployment never read runtime credentials. For a new
# environment only, pass one complete RUNTIME_DATABASE_URL directly to the
# single-target bootstrap command documented in DEPLOYMENT.md.
