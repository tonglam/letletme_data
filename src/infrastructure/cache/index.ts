/**
 * Cache Infrastructure Module
 *
 * Main entry point for the cache infrastructure layer.
 * Exports Redis implementation and cache types.
 *
 * Features:
 * - Redis cache implementation
 * - Cache type definitions
 * - Error handling types
 * - Operation interfaces
 *
 * This module serves as the public API for the cache infrastructure,
 * providing access to cache implementations and type definitions.
 */

export * from './redis';
export * from './types';
